{{- if .Values.workspace.persistent }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "happy-little-claude-coders.fullname" . }}-workspace
  labels:
    {{- include "happy-little-claude-coders.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.workspace.storageSize }}
---
{{- end }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "happy-little-claude-coders.fullname" . }}-gh-config
  labels:
    {{- include "happy-little-claude-coders.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.github.configPvcSize }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "happy-little-claude-coders.fullname" . }}-claude-config
  labels:
    {{- include "happy-little-claude-coders.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.claude.configPvcSize }}
